############################################
###       APPLICATION PARAMETERS         ###
############################################

# Database connection
spring.datasource.url=jdbc:mysql://arrowhead-core-mysql:3306/arrowhead?serverTimezone=Europe/Stockholm
spring.datasource.username=orchestrator
spring.datasource.password=KbgD2mTr8DQ4vtc
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
# use true only for debugging
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=none

# Orchestrator web-server parameters
domain.name=arrowhead-core
domain.port=8441
server.port=8441

############################################
###       CUSTOM PARAMETERS              ###
############################################

# Name of the core system
core_system_name=ORCHESTRATOR

# The network address this system should register itself with (MUST NOT be 0.0.0.0)
system.address=arrowhead-core
 
# Show all request/response in debug log
log_all_request_and_response=false

# Service Registry web-server parameters (to register the Orchestrator services)
sr_address=serviceregistry.testcloud2.aitia.arrowhead.eu
sr_port=8443

#If true, operational gatekeeper must be present
gatekeeper_is_present=true


spring.jpa.open-in-view=false


############################################
###           SECURE MODE                ###
############################################

server.ssl.enabled=false
server.ssl.key-store-type=PKCS12
server.ssl.key-store=/opt/arrowhead-core/certificates/orchestrator.p12
server.ssl.key-store-password=123456
server.ssl.key-alias=orchestrator
server.ssl.key-password=123456
server.ssl.client-auth=none
server.ssl.trust-store-type=PKCS12
server.ssl.trust-store=/opt/arrowhead-core/certificates/truststore.p12
server.ssl.trust-store-password=123456

disable.hostname.verifier=True

logging.level.eu.arrowhead=DEBUG